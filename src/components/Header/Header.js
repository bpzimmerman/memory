import React from "react";
import "./Header.css";

const Header = props => (
  <div id="ins">
    <h2>Memory Game!</h2>
    <h3>Click on an image to earn points, but don't click one any more than once!</h3>
  </div>
);

export default Header;